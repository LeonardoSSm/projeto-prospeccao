# Reaproveita toda a lógica/templates do tema padrão do Keycloak (login.ftl,
# fluxos de erro, i18n etc.) e troca só a aparência via CSS — nenhum
# comportamento de autenticação muda, só o visual.
parent=keycloak.v2
import=common/keycloak

styles=css/login.css
